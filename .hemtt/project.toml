mainprefix = "x"     # Ideally, replace "x" across the whole repo with your mod's mainprefix, example "t" (without the "")
prefix = "bluefields_armory"             # Ideally, replace "bluefields_armory" across the whole repo with your mod's prefix, example "mod" (without the "") 

name = "Bluefield's Armory"

[properties]
author = "Bluefield"
url = "https://github.com/bluefield-creator"

[binarize]
enabled = true # Default: true

[files]
include = [
    "mod.cpp",      # These files are copied to the build directory by default
    "meta.cpp",     # They do not need to be added to your list
    "LICENSE",
    "/img/*.paa",
    "logo_ca.paa",
    "logo_co.paa",
    "*.bikey",
]
exclude = [
    "*.psd",        # By default this list is empty
    "*.md",
    "/.vscode/",
    "/addons/template_addon/"
]

[version]
# path = "addons/main/script_version.hpp" # Default
# major = 0 # Overrides path when set
# minor = 1
# patch = 1
# git_hash = 4 # Default: 8
